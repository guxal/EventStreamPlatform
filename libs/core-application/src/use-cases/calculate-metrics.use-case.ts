export class CalculateMetricsUseCase {
    async execute(): Promise<void> {
      // Lógica para calcular y guardar métricas agregadas
      // Se puede disparar vía cron, scheduler o comando explícito
    }
  }